{% extends 'user/index.html.twig' %}

{% block titel %}Equipes index{% endblock %}

{% block body %}

    <div class="container-fluid">

        <!-- Page Heading -->
        <h1 class="h3 mb-2 text-gray-800">Mes Equipes</h1>

        <!-- DataTales Example -->
        <div class="card shadow mb-4">
            <div class="card-header py-3">
                <h6 class="m-0 font-weight-bold text-primary">Equipe dont je suis membres </h6>
            </div>
            <div class="card-body">
                <div class="table-responsive">
                    <div id="dataTable_wrapper" class="table table-striped table-bordered dt-bootstrap4">
                        <div class="row">
                            <div class="col-sm-12 col-md-6">


                                <div id="dataTable_filter" class="dataTables_filter"><label>Search:<input type="search" class="form-control form-control-sm" placeholder="" aria-controls="dataTable"></label></div></div></div><div class="row"><div class="col-sm-12">
                                <table class="table table-bordered dataTable" id="dataTable" width="100%" cellspacing="0" role="grid" aria-describedby="dataTable_info" style="width: 100%;">
                                    <thead>
                                    <tr>
                                        <th>Id</th>
                                        <th>Pseudo</th>
                                        <th>Capitaine</th>
                                        <th>actions</th>
                                    </tr>
                                  </thead>

                                    <tbody>
                                    {% for equipe in equipes %}
                                        {% if (user in equipe.membres)%}

                                            <tr>
                                                <td>{{ equipe.id }}</td>
                                                <td>{{ equipe.pseudo }}</td>
                                                <td>{{ equipe.capitaine.getUsername() }}</td>

                                                <td>

                                                    <a href="{{ path('equipes_show', {'id': equipe.id}) }}" class="btn btn-info btn-circle">
                                                        <i class="fas fa-info-circle"></i>
                                                    </a>

                                                </td>
                                            </tr>
                                        {% endif %}
                                    {% else %}
                                        <tr>
                                            <td colspan="3">no records found</td>
                                        </tr>

                                    {% endfor %}</tbody>
                                </table></div></div>
            </div>
        </div>

    </div>
    <!-- DataTales Example -->
    <div class="card shadow mb-4">
    <div class="card-header py-3">
        <h6 class="m-0 font-weight-bold text-primary">Equipe dont je suis capitaine </h6>
    </div>
    <div class="card-body">
        <div class="table-responsive">
            <div id="dataTable_wrapper" class="table table-striped table-bordered dt-bootstrap4">
                <div class="row">
                    <div class="col-sm-12 col-md-6">
                        <div id="dataTable_filter" class="dataTables_filter"><label>Search:<input type="search" class="form-control form-control-sm" placeholder="" aria-controls="dataTable"></label></div></div></div><div class="row"><div class="col-sm-12">
                        <table class="table table-bordered dataTable" id="dataTable" width="100%" cellspacing="0" role="grid" aria-describedby="dataTable_info" style="width: 100%;">

                        <thead>
                            <tr>
                                <th>Id</th>
                                <th>Pseudo</th>
                                <th>Capitaine</th>
                                <th>actions</th>
                            </tr>
                            </thead>

                            <tbody>
                            {% for equipe in equipes %}
                                {% if (username==equipe.capitaine.getUserName())%}

                                    <tr>
                                        <td>{{ equipe.id }}</td>
                                        <td>{{ equipe.pseudo }}</td>
                                        <td>{{ equipe.capitaine.getUsername() }}</td>
                                        <td><a href="{{ path('equipes_show', {'id': equipe.id}) }}" class="btn btn-info btn-circle">
                                                <i class="fas fa-info-circle"></i>
                                            </a>
                                            <a href="{{ path('equipes_edit', {'id': equipe.id}) }}" class="btn btn-warning btn-circle">
                                                <i class="fas fa-exclamation-triangle"></i>
                                            </a>
                                            <a href="{{ path('equipes_delete', {'id': equipe.id}) }}" class="btn btn-danger btn-circle">
                                                <i class="fas fa-trash"></i>
                                            </a>

                                        </td>
                                    </tr>
                                {% endif %}
                            {% else %}
                                <tr>
                                    <td colspan="3">no records found</td>
                                </tr>
                            {% endfor %}
                            </tbody>
                        </table></div></div>
            </div>
        </div>
        <div>

            <a href="{{ path('equipes_new') }}" class="btn btn-success btn-icon-split">
                                        <span class="icon text-white-50">
                                            <i class="fas fa-check"></i>
                                        </span>
                <span class="text">Ajouter </span>
            </a>

        </div>

    </div>
    </div>
        </div>
    </div>








{% endblock %}